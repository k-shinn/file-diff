name: Verify file-diff action

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: k-shinn/file-diff@master
        with:
          target: README.md
